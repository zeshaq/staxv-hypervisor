# Placeholder so //go:embed all:dist succeeds on a fresh clone before
# `make frontend` has been run. Everything else in this directory is
# gitignored — it's the Vite build output copied from ../../frontend/dist/.
